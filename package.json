{
  "name": "neuron",
  "version": "1.0.0",
  "description": "Landing Page fo NeuroN Foundation",
  "main": "main.js",
  "scripts": {
    "sass:compile": "node-sass --watch --recursive --output css --source-map true --source-map-contents sass",
    "sass:compress": "node-sass --watch --recursive --output css --source-map true --source-map-contents sass --output-style=compressed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.13.1"
  },
  "dependencies": {
    "compass": "^0.1.1",
    "compass-mixins": "^0.12.10"
  }
}
